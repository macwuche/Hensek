# Server
PORT=5000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-random-64-character-string-in-production

# Database (add when deploying to Replit)
DATABASE_URL=postgresql://postgres:password@localhost:5432/hensek

# Email (Resend)
RESEND_API_KEY=

# Email (SMTP fallback)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@hensek.com

# Default MD credentials (change after first login)
DEFAULT_MD_EMAIL=admin@hensek.com
DEFAULT_MD_PASSWORD=HensekAdmin2024!
